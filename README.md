# Skillock - Frontend Despliegue

Frontend estático para la plataforma Skillock.

Repositorio remoto: https://github.com/josepilco7501/Skillock-Frontend-Despliegue.git

Instrucciones rápidas:

```bash
# Inicializar y empujar (si no está hecho)
git init
git branch -M main
git remote add origin https://github.com/josepilco7501/Skillock-Frontend-Despliegue.git
git add .
git commit -m "Initial commit"
git push -u origin main
```
